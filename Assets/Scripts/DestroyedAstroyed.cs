using UnityEngine;
using System.Collections;

public class DestroyedAstroyed : MonoBehaviour {
	/*
	public GameObject explosion;
	private int cptB;	
	//private GController gameController;
	// Use this for initialization
	private BombeController gameController;
	void Start () {
		//cptB = 3;
		GameObject gameControllerObject = GameObject.FindGameObjectWithTag ("GameController");
		if (gameControllerObject != null) {
			gameController = gameControllerObject.GetComponent<BombeController> ();
		} else {
			Debug.Log("Composant GameController introuvable");
		}
	
	}
	// Update is called once per frame
	void Update () {

			if (Input.GetKeyDown (KeyCode.B)) {
			if (gameController.getCpt() >= 0){
				Destroy (gameObject);
				Instantiate (explosion, transform.position, transform.rotation);
				Debug.Log ("cptB: "+gameController.getCpt());
			}
		}
	}
	*/
}	